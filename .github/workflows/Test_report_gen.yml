name: Run Python Script on HTML Push

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    paths:
      - '**/*.html'

jobs:
  run-python-script:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        pip install -r requirements.txt

    - name: Run Python Script
      run: |
        python Test_data_extraction.py

    - name: Commit and push CSV file
      env:
        GITHUB_TOKEN: ${{ GRAFANA_POC.PERSONAL_ACCESS_TOKEN }}
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git add test_statistics.csv
        git commit -m "Add/update test_statistics.csv [automated]" || echo "No changes to commit"
        git push


